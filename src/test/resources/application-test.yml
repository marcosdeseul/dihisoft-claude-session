spring:
  datasource:
    url: jdbc:h2:mem:board-test;MODE=LEGACY;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: "test-secret-test-secret-test-secret-test-secret-test-secret-64!"
    expiration-ms: 60000
